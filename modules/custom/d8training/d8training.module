<?php
/**
 * @param $existing
 * @param $type
 * @param $theme
 * @param $path
 * @return array
 * hook_theme has been defined in .mould file only but it was not rendered during hands on in D8 trainign
 */
function d8training_theme($existing, $type, $theme, $path) {
  return array(
      'weather_widget' => array(
          'variables' => array('weather_data' => NULL),
      ),
  );
}

